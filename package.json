{
  "name": "maou-gambit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:pages": "cross-env GITHUB_PAGES=1 vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "cross-env": "^10.1.0",
    "vite": "^7.1.7"
  }
}
